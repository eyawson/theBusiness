# Copy to .env (committed defaults) or .env.local (personal override, gitignored).
# This URL is public — it ships in the JS bundle. Keeping it in env makes
# stage promotion (dev -> prod) a config change, not a code change.
VITE_CONTACT_API_URL=https://your-api-id.execute-api.us-east-1.amazonaws.com/dev/contact
